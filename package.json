{
  "name": "vantage.nvim",
  "description": "A Neovim-first AI review and learning assistant with a local TypeScript backend.",
  "author": "Nick Pisani",
  "license": "MIT",
  "version": "0.0.1",
  "repository": {
    "type": "git",
    "url": "https://github.com/napisani/learn-lsp"
  },
  "private": true,
  "keywords": [
    "neovim",
    "ai",
    "code-review",
    "learning"
  ],
  "scripts": {
    "build:plugin": "npm run compile && node scripts/build-plugin-package.mjs",
    "compile": "tsc -b server",
    "watch": "tsc -b server -w",
    "lint": "eslint \"server/src/neovim/**/*.ts\"",
    "test": "npm run test:mvp",
    "dev": "nodemon --watch server/src --exec \"npm run compile\"",
    "test:backend": "npm run compile && node --test server/out/neovim/*.test.js",
    "test:nvim": "mkdir -p .nvim-dev/state/nvim && XDG_STATE_HOME=.nvim-dev/state NVIM_LOG_FILE=.nvim-dev/state/nvim/test.log nvim --headless -u nvim/tests/minimal_init.lua -c \"lua require('vantage_spec').run()\" -c qa",
    "test:mvp": "npm run test:backend && npm run test:nvim"
  },
  "devDependencies": {
    "@eslint/js": "^9.13.0",
    "@stylistic/eslint-plugin": "^2.9.0",
    "@types/node": "^22",
    "eslint": "^9.13.0",
    "nodemon": "^3.1.11",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.39.0"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "^0.74.0",
    "openai": "^6.37.0"
  }
}
